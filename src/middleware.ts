/**
 * Microbot
 * Copyright James Campbell 2017
 */

/**
 * 
 * 
 * @export
 * @class Middleware
 */
export class Middleware {

    /**
     * 
     * 
     * @param {*} event 
     * @returns 
     * @memberof Middleware
     */
    before(event: any) {
        return event
    }

    /**
     * 
     * 
     * @param {*} event 
     * @returns 
     * @memberof Middleware
     */
    after(event: any) {
        return event
    }
}